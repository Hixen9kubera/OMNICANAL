# ───────────────────────────────────────────────────────────────
#  SECRETOS — NUNCA subir a GitHub. En Railway van como variables.
# ───────────────────────────────────────────────────────────────
.env
.env.*
!.env.example
!.env.*.example
# variantes SIN punto inicial (env, env.railway, env.amazon, env.staging...)
# — contienen las mismas credenciales y el patrón .env* NO las cubre
env
env.*
!env.example
!env.*.example
*.pem
config/meli_tokens.json

# ── Python / backend ───────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ── Node / frontend ────────────────────────────────────────────
node_modules/
.next/
out/
.turbo/
npm-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ── Sistema / editor ───────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log
tsconfig.tsbuildinfo

# WordPress config (secretos)
wp-config*.php

# Repos externos de referencia (no forman parte de este proyecto)
publicaciones_ready/
publicaciones_amazon/

# Exportes de datos de trabajo
fantasmas_*.csv

# Respaldos de base de datos (contienen tokens/credenciales — NUNCA subir)
backups/
*.bak

# volcados temporales de auditorías (no son código)
audita_*.json
auditoria*.json
fanout_sobreventa.json
ambiguos.json

# Base local del módulo de Competencia (datos desechables, se reescriben cada mes)
backend/competencia.db

# Perfil de Chrome del raspador de Competencia: trae la SESIÓN de la cuenta
# dedicada de Mercado Libre. Nunca al repo. Vive en ~/.competencia-chrome,
# pero se ignora también aquí por si alguien lo copia al proyecto.
.competencia-chrome/
