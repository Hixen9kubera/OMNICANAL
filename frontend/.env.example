# Copiar a .env.local (local) o configurar en Railway.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Panel SIN login. Solo para el sandbox/local, donde la BD de pruebas no tiene
# a nadie dado de alta (las cuentas viven en el Supabase de producción).
# NO definir en producción: se sustituye al compilar, así que basta con no
# ponerla para que el guardia quede activo.
# NEXT_PUBLIC_AUTH_OFF=true
