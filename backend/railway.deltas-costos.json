{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install pymysql psycopg2-binary"
  },
  "deploy": {
    "startCommand": "python scripts/comparar_costos.py",
    "cronSchedule": "30 6 * * *",
    "restartPolicyType": "NEVER"
  }
}
