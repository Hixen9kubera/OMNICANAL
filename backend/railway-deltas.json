{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install pymysql psycopg2-binary"
  },
  "deploy": {
    "startCommand": "python scripts/comparar_orders.py",
    "restartPolicyType": "NEVER"
  }
}
